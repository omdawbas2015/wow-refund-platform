apiVersion: v1
kind: Service
metadata:
  name: wow-redis
  namespace: wow-refund
  labels:
    app.kubernetes.io/name: wow-refund
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app.kubernetes.io/name: wow-refund
    app.kubernetes.io/component: redis
